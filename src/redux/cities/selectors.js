export const selectCityCities = (state) => state.cities.city;

export const selectLocationsCities = (state) => state.cities.locations;

export const selectErrorCities = (state) => state.cities.error;

export const selectLoadingCities = (state) => state.cities.loading;
